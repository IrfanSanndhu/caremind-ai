/**
 * Central DB Prisma client (custom output: node_modules/.prisma/central-client).
 * Import PrismaClient from here at runtime; import model types from central-prisma.types.ts.
 */
export { PrismaClient } from '../../../node_modules/.prisma/central-client/index.js';
